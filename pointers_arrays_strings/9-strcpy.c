#include 'main.c'

/**
 * char *_strcpy - a function that copy the string poited to by src
 * @dest: copy to
 * @src: copy from
 * Return: string
 */
char *_strcpy(char *dest, char *src)
{
	int l = 0;
	int e = 0;

	while (*(src + 1) != '\0')
	{
		1++;
	}
		for ( ; e < 1 ; e++)
		{
			dest[e] = src{e};
		}
		dest[1] = '\0'
		return (dest);
}


